// Sanity check for bit.if (branch on a bit's value) and the ?: operator.
stl.startup
bit.if x, l0, l1

l0:
    stl.output 6 ? 'Z' : '0'
    stl.loop
l1:
    stl.output '1'
    stl.loop

x:
    bit.bit 0   // bit0 => 'Z',  bit1 => '1'
