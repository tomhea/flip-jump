// Demo Rep 5 Stars (#1261)

// No input. Uses `rep(5, i) stl.output('*')` then `stl.output('\n')` to print `*****\n`.

main

def main {
    stl.startup
    rep(5, i) stl.output '*'
    stl.output '\n'
    stl.loop
}
