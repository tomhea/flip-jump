// Demo Macro One Arg (#0862)

// Defines a macro `print_byte(b)` that calls `stl.output(b)`. The top-level invokes `print_byte('A')`. Prints `A\n`.

main

def main {
    stl.startup
    print_byte 'A'
    stl.output '\n'
    stl.loop
}

// Output one byte.
def print_byte b {
    stl.output b
}
