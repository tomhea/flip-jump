// Shift Left One (#0202)

// Reads one byte and outputs `byte << 1` (low bit becomes 0; high bit is lost).

main

def main < ch {
    stl.startup
    bit.input ch
    bit.shl 8, ch
    bit.print ch
    stl.loop
}

ch: bit.vec 8, 0
