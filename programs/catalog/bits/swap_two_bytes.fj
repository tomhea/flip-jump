// Swap Two Bytes (#0896)

// Reads exactly 2 bytes. Outputs them in reverse order (byte2 first, then byte1) as raw bytes.

main

def main < a, b {
    stl.startup
    bit.input a
    bit.input b
    bit.print b
    bit.print a
    stl.loop
}

a: bit.vec 8, 0
b: bit.vec 8, 0
