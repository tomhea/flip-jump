// Or Two Bytes (#0201)

// Reads exactly two bytes and outputs their bitwise OR as one byte.

main

def main < a, b {
    stl.startup
    bit.input a
    bit.input b
    bit.or 8, a, b
    bit.print a
    stl.loop
}

a: bit.vec 8, 0
b: bit.vec 8, 0
