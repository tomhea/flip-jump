// Count Primes To N (#0116)

// Reads decimal N in `0`-`50` and prints the count of prime numbers in `[2..N]` as decimal + `\n`.

main

def main @ loop, body, next, done < limit, cand, count, flag, two {
    stl.startup
    read_decimal 16, limit
    bit.zero 16, count
    bit.mov 16, cand, two
  loop:
    bit.cmp 16, cand, limit, body, body, done
  body:
    is_prime_into 16, flag, cand
    bit.if0 flag, next
    bit.inc 16, count
  next:
    bit.inc 16, cand
    ;loop
  done:
    bit.print_dec_uint 16, count
    stl.output '\n'
    stl.loop
}

// Read an unsigned decimal (digits until `\n` or `\0`) into value[:n].
def read_decimal n, value @ loop, add_digit, end < ch, nl, digit, err {
    bit.zero n, value
  loop:
    bit.input ch
    bit.if0 8, ch, end
    bit.cmp 8, ch, nl, add_digit, end, add_digit
  add_digit:
    bit.mul10 n, value
    bit.ascii2dec err, digit, ch
    bit.add n, value, digit
    ;loop
  end:
}

// flag = 1 if x[:n] is prime (x >= 2), else 0. Trial division by 2..x-1.
def is_prime_into n, flag, x @ check, loop, test, composite, prime, end < d, q, r, two {
    bit.cmp n, x, two, composite, check, check
  check:
    bit.mov n, d, two
  loop:
    bit.cmp n, d, x, test, prime, prime
  test:
    bit.div n, x, d, q, r
    bit.if0 n, r, composite
    bit.inc n, d
    ;loop
  composite:
    bit.zero flag
    ;end
  prime:
    bit.one flag
  end:
}

limit: bit.vec 16, 0
cand: bit.vec 16, 0
count: bit.vec 16, 0
flag: bit.bit
d: bit.vec 16, 0
q: bit.vec 16, 0
r: bit.vec 16, 0
two: bit.vec 16, 2
ch: bit.vec 8, 0
nl: bit.vec 8, '\n'
digit: bit.vec 16, 0
err: bit.bit
