// Mul By 7 (#0111)

// Reads one decimal in `0`-`14` and prints `a * 7` as decimal + `\n`.

main

def main < a, k, p, input_err {
    stl.startup_and_init_all
    hex.input_dec_uint 4, a, input_err
    hex.mul 4, p, a, k
    hex.print_dec_uint 4, p
    stl.output '\n'
    stl.loop
}

a: hex.vec 4, 0
k: hex.vec 4, 7
p: hex.vec 4, 0

input_err:
    stl.loop
