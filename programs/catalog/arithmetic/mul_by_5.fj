// Mul By 5 (#0110)

// Reads one decimal in `0`-`20` and prints `a * 5` as decimal + `\n`.

main

def main < a, k, p, input_err {
    stl.startup_and_init_all
    hex.input_dec_uint 4, a, input_err
    hex.mul 4, p, a, k
    hex.print_dec_uint 4, p
    stl.output '\n'
    stl.loop
}

a: hex.vec 4, 0
k: hex.vec 4, 5
p: hex.vec 4, 0

input_err:
    stl.loop
